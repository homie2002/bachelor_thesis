#include <Servo.h>

// Создаем массив объектов Servo для управления четырьмя сервоприводами
Servo servos[4];

// Пины, к которым подключены сервоприводы
const int servoPins[4] = {3, 5, 6, 9};

// Начальные углы для каждого сервопривода
int angles[4] = {90, 90, 90, 90};

// Задержка между движениями в миллисекундах
const int delayTime = 500;

void setup() {
  // Присоединяем каждый сервопривод к соответствующему пину
  for (int i = 0; i < 4; i++) {
    servos[i].attach(servoPins[i]);
    servos[i].write(angles[i]); // Устанавливаем начальное положение
    delay(500); // Небольшая задержка для стабилизации
  }
}

void loop() {
  // Пример последовательных движений сервоприводов
  for (int i = 0; i < 4; i++) {
    // Поворачиваем сервопривод на 45 градусов
    angles
